<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jalil.bimarstan">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".G"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyDnlENmnICEs5FaDyuZbLZ9Jvyk0cyu0A0" />

        <service
            android:name=".service.MessageService"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".LangaugeActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Chatall_socketActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".VisitActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".LoginActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Signup2Activity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".forget"
            android:screenOrientation="portrait" />
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".VisitAnswerAzmoonActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".tabel"
            android:screenOrientation="portrait" />
        <activity
            android:name=".TurnsearchActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".MmpiActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".TurnActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".jalil"
            android:screenOrientation="portrait" />
        <activity
            android:name=".AzmoonActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".test"
            android:screenOrientation="portrait" />
        <activity android:name=".DepressActivity" />
        <activity android:name=".AnogramActivity" />
        <activity android:name=".VisitorActivity" />
        <activity android:name=".Detail_darogeramActivity" />

        <receiver
            android:name=".service.autostart"
            android:enabled="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <activity android:name=".ShowImageChatActivity" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <!-- <meta-data -->
        <!-- android:name="com.google.android.geo.API_KEY" -->
        <!-- android:value="@string/google_maps_key" /> -->

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".Dragestor_listActivity" />
        <activity android:name=".List_doctorsActivity" />
        <activity android:name=".SignupActivity"></activity>
    </application>

</manifest>